CREATE TABLE test2763_book (
    id FIXED(20) not null,
    title VARCHAR(100) NOT NULL,
    ts FIXED(20) NOT NULL,
     PRIMARY KEY (id)
)
//

CREATE TABLE test2763_employee (
    id FIXED(20) not null,
    name VARCHAR(100) NOT NULL,
    version FIXED(20) NOT NULL,
    primary key (id)
)
//